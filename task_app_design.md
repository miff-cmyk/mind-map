# タスク管理アプリ 設計図・手順書

マインドマップ × ガントチャート連携型タスク管理アプリ
副業フルスペック版 / 本業SharePoint版 共通設計

最終更新: 2026-05-23

---

## 0. このドキュメントの使い方

2つの目的を1つの設計から派生させるための地図。

- **副業版**: 一人用・Claude Code環境でフルスペックに作り込む。学習目的も兼ねる。
- **本業版**: チーム用・SharePoint上で共有。会社はClaude非課金（=Claude Codeは使わず、副業版で作ったファイルを持ち込む）。

両方とも「同じデータの形」「同じ画面構成」を共有する。違うのは**保存先だけ**。
だから最初にデータの形を固め、保存先を差し替え可能な作りにしておくことが最重要。

このファイルはClaude Codeにそのまま読ませる指示書としても使える。

---

## 1. アプリの核となる考え方

マインドマップとガントチャートは「連携」というより**1つのデータの2つの顔**。

1つのタスクが以下を全部持つ:

- マインドマップが使う情報 → 親子関係・タイトル
- ガントチャートが使う情報 → 開始日・終了日・進捗率

同じタスク配列を、ツリー状に描けばマインドマップ、横棒で描けばガントになる。
データは1つ。ビューが2つ。これが全体を貫く設計思想。

---

## 2. データの形（最重要・両版共通）

タスク1件が持つ項目。ここを最初に固定し、以降一切ぶらさない。

| 項目 | キー名 | 型 | 説明 |
|------|--------|----|----|
| ID | `id` | 文字列 | 一意。重複しない値（例: UUID） |
| 親ID | `parentId` | 文字列/null | 親タスクのID。ルートは null |
| 名前 | `name` | 文字列 | タスク名 |
| 開始日 | `start` | 文字列 | "YYYY-MM-DD" 形式 |
| 終了日 | `end` | 文字列 | "YYYY-MM-DD" 形式 |
| 進捗 | `progress` | 数値 | 0〜100 |
| 表示色 | `color` | 文字列 | 任意。系統ごとの色分け用 |
| 並び順 | `order` | 数値 | 同じ親の中での表示順 |

### 最小版での確定事項（2026-05-23 決定）

最初の最小版では以下の方針で確定:

- **入れる**: id / parentId / name / start / end / progress / **color（色分け）** / order
- **後回し（設計だけ空けておく）**: メモ欄（`note`） / 依存関係（`dependsOn`）

理由: 色分けは実装コストが低く視認性への効果が高い。メモは当面 name で代用可能。
依存関係は「Aが遅れたらBもずれる」等の計算が絡み複雑化するため、本格ガントが
必要になった段階で追加する。`version` を持たせてあるので後からの項目追加は安全。

JSONでの1件の例:

```json
{
  "id": "t-001",
  "parentId": null,
  "name": "アプリ開発プロジェクト",
  "start": "2026-06-01",
  "end": "2026-08-31",
  "progress": 10,
  "color": "purple",
  "order": 0
}
```

アプリ全体のデータは、この配列。

```json
{
  "version": 1,
  "updatedAt": "2026-05-23T10:00:00Z",
  "tasks": [ /* 上記の配列 */ ]
}
```

> `version` と `updatedAt` を最初から入れておくこと。
> 後でデータ形式を変えたときの移行や、SharePointでの「どちらが新しいか」判定に効く。

---

## 3. 画面構成（両版共通）

3つの領域。

1. **マインドマップ領域** — 中心ノードから枝分かれ。ノードのドラッグ・追加・折りたたみ。
2. **ガントチャート領域** — 各タスクを横棒で時系列表示。進捗を濃淡で表現。
3. **編集パネル** — 選択中タスクの名前・日付・進捗を編集。子タスク追加・削除。

連動のルール:

- どのビューでタスクを選んでも、両ビューで選択がハイライトされる。
- 編集パネルでの変更は、両ビューに即反映される。
- これは「同じデータ配列を見ている」から自然に成立する。

### 画面構成の確定事項（2026-05-23 決定）

レイアウト = **タブ切り替え方式**（マインドマップ / ガントを一方ずつ大きく表示）。

- 理由: 本業版でチームの画面サイズがばらつくため、どの環境でも一方を最大限
  広く使えるタブ方式が堅実。マインドマップを大きく描きたい用途にも合う。
- 連携感の担保: **選択中タスクはタブを切り替えても保持する**。マインドマップで
  選んだタスクがガントタブでも選択状態のままになるようにし、「同じものを別角度で
  見る」感覚を維持する。
- 編集パネルはどちらのタブでも下部か側面にスッと出る共通UIとする。
- 注: 画面の並べ方は「見せ方」の決定なので、後から左右分割や上下分割へ変更しても
  影響は小さい（データの形と違い、変更コストが低い）。

---

## 4. アーキテクチャ（差し替え可能な設計）

肝は **保存層を本体から切り離す** こと。

```
┌─────────────────────────────────┐
│  画面（マインドマップ / ガント / 編集）  │  ← 両版で完全に同じ
├─────────────────────────────────┤
│  タスク操作ロジック（追加・編集・削除）  │  ← 両版で完全に同じ
├─────────────────────────────────┤
│  保存層（Storage）  ★ここだけ差し替える★  │
│   ・副業版   → IndexedDB（ブラウザ内DB）  │
│   ・本業版   → SharePoint（ファイル/リスト）│
└─────────────────────────────────┘
```

保存層は以下の4つの操作だけ持つ「お約束（インターフェース）」にする:

- `load()` … 全タスクを読み込む
- `save(tasks)` … 全タスクを保存する
- `export()` … JSONを書き出す（手動同期・バックアップ用）
- `import(json)` … JSONを読み込む

この4つさえ実装すれば、中身がIndexedDBでもSharePointでも本体は気にしない。
**これが「同じアプリで保存先だけ違う」を実現する仕掛け。**

### 保存層の確定事項（2026-05-23 決定）

最小版から4操作すべてを実装する方針で確定:

- `load()` … 全タスクを読み込む（必須・アプリ起動時）
- `save(tasks)` … 全タスクを保存する（必須・編集時）
- `export()` … JSONを書き出す（バックアップ＆本業版の手動同期で使用）
- `import(json)` … JSONを読み込む（exportの相棒）

理由: export/import は実装が軽い割に、副業版→本業版への橋渡し（書き出した
JSONをSharePoint共有フォルダに持ち込む）がそのまま実現できる。最初から入れておくと
本業版着手時に「もう仕組みがある」状態になる。

---

## 5. 技術スタック

### 副業フルスペック版

| 役割 | 採用技術 | 理由 |
|------|---------|------|
| 土台 | React + Vite | Claude Code学習の王道。情報量が多い |
| マインドマップ | React Flow | ドラッグ・ズーム・線の追従が最初から揃う |
| ガントチャート | 自作 or 軽量ライブラリ | データ構造が共通なので自作も容易 |
| 保存 | IndexedDB | ブラウザ内に大量データを保存できる |
| 仕上げ | （任意）クラウド保存・通知連携 | フェーズ2以降 |

### 本業SharePoint版

| 役割 | 採用技術 | 理由 |
|------|---------|------|
| 形態 | 単一HTMLファイル | アップロードするだけで動く。ビルド不要 |
| 描画 | CDN読み込みのライブラリ or 軽量自作 | SharePoint上で重くしない配慮 |
| 保存 | レベルA→B→Cで段階的（後述） | 権限と相談しながら |

> 本業版は「副業版で作ったReactアプリをビルドして1ファイルにまとめたもの」にもできる。
> ただし最初はシンプルな単一HTMLから始めるのが安全。

---

## 6. 段階的ロードマップ

### 共通の出発点（フェーズ0）

- データの形（第2章）を確定する ← まずここ
- 保存層インターフェース（第4章の4操作）を決める
- 画面構成（第3章）のたたき台を作る

### 副業版のロードマップ

| フェーズ | 内容 | ゴール |
|---------|------|--------|
| 1 | React + React Flowでマインドマップ＆ガント | ブラウザ内で動く（IndexedDB保存） |
| 2 | クラウド保存（任意のDB） | 複数端末から見られる |
| 3 | Googleカレンダー連携 | 締切をカレンダーに自動登録 |
| 4 | Chatwork連携 | 締切前に通知 |

### 本業版のロードマップ（SharePoint）

| レベル | 内容 | 共有範囲 | 権限ハードル |
|--------|------|---------|-------------|
| A | HTMLファイルを共有ライブラリに置く | アプリの共有のみ（データは各自） | なし。今すぐ可能 |
| B | データJSONをライブラリに読み書き | データもチームで共有 | 中（後述の回避策あり） |
| C | SharePointリストに1件1行で保存 | 同時編集・変更履歴も | 高（情シス相談の可能性） |

---

## 7. 本業SharePoint版の詳細メモ

### 大前提（てつさんの環境）

- 会社はClaude非課金 → **Claude Codeは本業では使わない**。
- アプリは副業のClaude Code環境で作り、**完成ファイルを本業SharePointに持ち込む**。
- 本業側にClaude環境は一切不要。
- てつさんの権限 = 一般ユーザーだが**サイト作成・ファイル配置はできる**。

### レベルA（今すぐ確実にできる）

- 完成HTMLファイルを共有ドキュメントライブラリにアップロード。
- チームはファイルを開けばアプリが使える。サイトのページ埋め込みも可。
- 制約: 各自の編集データは各自のブラウザ内に留まる（=データ共有はまだ）。

### レベルB（データ共有・本命）

データを `tasks.json` としてSharePointライブラリに置き、読み書きする。
権限の壁を越える2つの道:

1. **ログイン本人の権限を使う方式**
   チームは会社アカウントでログイン済みの状態でアプリを開く。
   アプリはその本人としてファイルを読み書きするので、
   新規の外部アプリ許可なしで本人がアクセスできる範囲には届く可能性。
   → 作り込みは要るが情シス承認を避けられる見込み。

2. **手動同期方式（確実・泥臭い）**
   アプリに「書き出し／読み込み」ボタンを付ける。
   編集→JSON書き出し→共有フォルダに置く。開く人は読み込む。
   完全自動ではないが権限問題ゼロ。少人数なら十分回る。

> どちらが御社で通るかは作って試さないと確定しない。「作りながら確かめる」領域。
> まずレベルA→手応えがあればレベルB（手動同期から）が安全な順序。

### レベルC（本格運用）

- SharePointの「リスト」にタスクを1件1行で保存。
- 同時編集に強く、変更履歴も残る。
- 構築の手間は最大。情シス相談が必要になる場合あり。

---

## 8. 設計上の重要な約束ごと

最初に決めて、最後まで守る原則。後から効いてくる。

1. **データの形を絶対にぶらさない**（第2章）。両版・全フェーズで同じ。
2. **保存層は本体から切り離す**（第4章）。保存先の違いをここに閉じ込める。
3. **`version` と `updatedAt` を最初から持つ**。移行と新旧判定のため。
4. **本業版は「重さ」に配慮**。チームの端末環境は様々。軽い構成から。
5. **段階を飛ばさない**。レベルA→B→C、フェーズ1→2→3。土台が固まってから次へ。

---

## 9. 次のアクション

このドキュメントを土台に、どちらかを選んで着手する。

- **副業版を選ぶ場合**: フェーズ0（データの形・保存層）を確定 → React + React Flowで
  フェーズ1の実装へ。Claude Codeにこのファイルを読ませて進める。
- **本業版を選ぶ場合**: レベルAの単一HTMLファイルを仕上げる（手動同期=export/import付き）。
  副業のClaude Code環境で作り、本業SharePointにアップロードしてチームで試す。

どちらを選んでも、第2章のデータの形が共通の出発点になる。
